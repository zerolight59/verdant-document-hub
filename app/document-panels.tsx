'use client';
import { CheckCircle2,Eye,FileCheck2,FileText,Link2,Sparkles,Upload,XCircle } from 'lucide-react';
import { Badge } from '@/components/ui/badge'; import { Button } from '@/components/ui/button'; import { Label } from '@/components/ui/label';
import { Sheet,SheetContent,SheetDescription,SheetHeader,SheetTitle } from '@/components/ui/sheet'; import { Tabs,TabsContent,TabsList,TabsTrigger } from '@/components/ui/tabs'; import { Textarea } from '@/components/ui/textarea';
import { auditEvents,ProjectDocument,ResearchFile } from './prototype-data'; import { Endorsement,FilePreview,Person,StatusBadge } from './prototype-components';
export function ProjectDocumentPanel({document,onClose,onDecision}:{document:ProjectDocument|null;onClose:()=>void;onDecision:(status:'Approved'|'Changes requested')=>void}){return <Sheet open={Boolean(document)} onOpenChange={open=>!open&&onClose()}><SheetContent className="w-full gap-0 overflow-y-auto p-0 sm:max-w-2xl">{document&&<><SheetHeader className="border-b border-slate-200 px-6 py-5 pr-14"><div className="mb-2 flex flex-wrap items-center gap-2"><StatusBadge status={document.status}/><Badge variant="secondary">{document.version}</Badge><Badge variant="outline">Project X · {document.stage}</Badge></div><SheetTitle className="text-xl font-semibold">{document.name}</SheetTitle><SheetDescription>{document.description}</SheetDescription></SheetHeader><Tabs defaultValue="preview" className="min-h-0 flex-1 gap-0"><TabsList variant="line" className="mx-6 mt-3 h-10"><TabsTrigger value="preview" className="px-3">Preview</TabsTrigger><TabsTrigger value="versions" className="px-3">Versions</TabsTrigger><TabsTrigger value="review" className="px-3">Review</TabsTrigger><TabsTrigger value="activity" className="px-3">Activity</TabsTrigger></TabsList><TabsContent value="preview" className="p-6"><FilePreview title={document.name}/></TabsContent><TabsContent value="versions" className="p-6"><div className="space-y-3">{document.version==='—'?<div className="rounded-xl border border-dashed border-slate-300 p-8 text-center"><Upload className="mx-auto size-6 text-slate-400"/><p className="mt-3 font-medium">No file uploaded yet</p><p className="mt-1 text-sm text-slate-500">The responsible person can create version 1.</p></div>:[document.version,'v1'].filter((v,i,a)=>a.indexOf(v)===i).map((version,index)=><div key={version} className="flex items-center gap-3 rounded-xl border border-slate-200 p-4"><span className={`flex size-10 items-center justify-center rounded-full ${index===0?'bg-emerald-100 text-emerald-700':'bg-slate-100 text-slate-500'}`}><FileText className="size-4"/></span><div className="flex-1"><p className="font-medium">{version} {index===0&&'· Current version'}</p><p className="mt-1 text-sm text-slate-500">Uploaded by {document.responsible} · {document.updated}</p></div><Button variant="ghost" size="icon-sm"><Eye/></Button></div>)}</div></TabsContent><TabsContent value="review" className="space-y-5 p-6"><div className="grid gap-3 sm:grid-cols-2"><div className="rounded-xl border border-slate-200 p-4"><p className="text-xs font-semibold uppercase tracking-wide text-slate-400">Responsible person · Edit</p><div className="mt-3"><Person name={document.responsible} initials={document.responsibleInitials}/></div></div><div className="rounded-xl border border-slate-200 p-4"><p className="text-xs font-semibold uppercase tracking-wide text-slate-400">Reviewer · Review</p><div className="mt-3"><Person name={document.reviewer} initials={document.reviewerInitials}/></div></div></div>{document.status==='Under review'||document.status==='Changes requested'?<div className="rounded-xl border border-slate-200 p-4"><Label htmlFor="review-comment">Review comment</Label><Textarea id="review-comment" className="mt-2 min-h-28" placeholder="Record findings, requested changes, or approval notes…"/><div className="mt-3 flex flex-col gap-2 sm:flex-row sm:justify-end"><Button variant="outline" onClick={()=>onDecision('Changes requested')} className="border-rose-200 text-rose-700 hover:bg-rose-50"><XCircle/>Request changes</Button><Button onClick={()=>onDecision('Approved')} className="bg-[#1b5e45] hover:bg-[#144b37]"><FileCheck2/>Approve version</Button></div></div>:<div className="rounded-xl border border-slate-200 bg-slate-50 p-4 text-sm text-slate-600">Review actions become available after the responsible person submits a version.</div>}<div className="rounded-xl border border-emerald-100 bg-emerald-50/60 p-4"><div className="flex gap-3"><Sparkles className="mt-0.5 size-5 text-emerald-700"/><div><p className="text-sm font-medium text-slate-800">Separation of responsibility enforced</p><p className="mt-1 text-xs leading-5 text-slate-500">The responsible person and reviewer cannot be the same employee.</p></div></div></div></TabsContent><TabsContent value="activity" className="p-6"><div>{auditEvents.slice(0,4).map(event=><div key={event.detail} className="flex gap-3 border-b border-slate-100 py-3 last:border-0"><span className="flex size-8 items-center justify-center rounded-full bg-slate-100"><CheckCircle2 className="size-3.5"/></span><div><p className="text-sm font-medium">{event.action}</p><p className="mt-0.5 text-xs text-slate-500">{event.detail}</p><p className="mt-1 text-xs text-slate-400">{event.time}</p></div></div>)}</div></TabsContent></Tabs></>}</SheetContent></Sheet>}
export function ResearchPanel({file,onClose}:{file:ResearchFile|null;onClose:()=>void}){return <Sheet open={Boolean(file)} onOpenChange={open=>!open&&onClose()}><SheetContent className="w-full gap-0 overflow-y-auto p-0 sm:max-w-2xl">{file&&<><SheetHeader className="border-b border-slate-200 px-6 py-5 pr-14"><div className="mb-2 flex items-center gap-2"><Badge className="bg-sky-50 text-sky-700 hover:bg-sky-50">Research library</Badge>{file.endorsed&&<Endorsement label={file.endorsed}/>}</div><SheetTitle className="text-xl font-semibold">{file.name}</SheetTitle><SheetDescription>{file.category} · Uploaded by {file.uploadedBy}</SheetDescription></SheetHeader><div className="p-6"><FilePreview title={file.name} kind="Company research file"/><div className="mt-4 flex items-center justify-between rounded-xl border border-slate-200 p-4"><div className="flex items-center gap-3"><Link2 className="size-5 text-emerald-700"/><div><p className="text-sm font-medium">Linked to Project X</p><p className="mt-1 text-xs text-slate-500">Visible from the project’s related research section</p></div></div><Badge variant="outline">View access</Badge></div></div></>}</SheetContent></Sheet>}
